#include <iostream>

#include "application.h"
#include "menu.h"
#include "map/map.h"

int main() {
    Application app;
    app.run();
    // Map map = Map(10, 20);
    // Position someonsesPosition = Position(1, 1);
    // map(someonsesPosition) = 'A';
    // map.print();
}
