#include <iostream>

#include "application.h"
#include "menu.h"
#include "map/map.h"

int main() {
    Application app;
    app.run();
    // Map map = Map(Position{10, 20});
    // map(0, 0) = 'A';
    // map.print();
}
